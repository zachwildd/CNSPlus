namespace CNSPlus.Web.Tests;

// Placeholder. Real tests arrive in phase 3 (public reader pages) — bUnit renders of
// SSR components with a mocked API client.
public class SmokeTests
{
    [Fact]
    public void Project_compiles_and_test_runner_works()
    {
        Assert.True(true);
    }
}
